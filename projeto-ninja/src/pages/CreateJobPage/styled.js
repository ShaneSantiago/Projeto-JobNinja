import styled from "styled-components"

export const CreateJobContainer = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
input{
    padding: 10px;
}
button{
    padding: 10px;
}
`